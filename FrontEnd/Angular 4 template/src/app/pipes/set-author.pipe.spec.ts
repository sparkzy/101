import { SetAuthorPipe } from './set-author.pipe';

describe('SetAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new SetAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
