import { QuizAuthorPipe } from './quiz-author.pipe';

describe('QuizAuthorPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizAuthorPipe();
    expect(pipe).toBeTruthy();
  });
});
