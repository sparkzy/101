import { SubjectPostPipe } from './subject-post.pipe';

describe('SubjectPostPipe', () => {
  it('create an instance', () => {
    const pipe = new SubjectPostPipe();
    expect(pipe).toBeTruthy();
  });
});
