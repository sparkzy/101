import { SetSubjectPipe } from './set-subject.pipe';

describe('SetSubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new SetSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
