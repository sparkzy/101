import { QuizSubjectPipe } from './quiz-subject.pipe';

describe('QuizSubjectPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizSubjectPipe();
    expect(pipe).toBeTruthy();
  });
});
